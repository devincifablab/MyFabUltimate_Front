- Corriger les problèmes de fetch d'api trop nombreux --> centraliser tout
- Idem pour les layout et tout
- Finir la partie locker
- Terminer la page settings
- Cacher l'ancienne page de connexion
- Une super route pour avoir tous les blob des tickets pas traités genre:
{
  {
    id: 2,
    files:{
      [url: ...],
      [url: ...]
    },
    step: 3
  },
  {
    id: 3,
    files:{
      [url: ...]
    },
    step: 1
  },
  ........
}

- et quand on a terminé la TODO list, on upgrade tailwind vers la v3 --> https://tailwindcss.com/docs/upgrade-guide
- Regardeer du côté de Ghost pour faire le blog (ce serait bien lol)
- SEO lol

Au niveau de l'API:
- ça bug pour changer le mdp d'un compte lol (internal server error) avec la route /user/password/:id
- Il me manque le système de rester password -> la page est là mais jsp si ça marche (L'endpoint 'api/user/resetPassword/{tocken}' est un PUT pas un POST)
- Pareil système pour valider le compte -> il n'y a pas de page pour valider pour l'instant (Pour ca c'est un PUT 'api/user/resetPassword/{tocken}')
- Une route pour supprimer le ticket (C'est fait)

- sur la page 'register' mettre une confirmation de mot de passe (comme pour la page 'auth/password/{tocken}' ca marche nickel comme ca).
